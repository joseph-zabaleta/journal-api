build:
    docker:
        web: journal_project/Dockerfile
release:
    image: web
    command:
        - mkdir staticfiles
        - python manage.py collectstatic --noinput
run:
    web: gunicorn config.wsgi
